# Claude Peers 사용법

동료가 맡은 레포의 내부 동작을 알아야 하는데 내 워크스페이스에서는 확인할 수 없을 때, 그 레포에 띄워 둔 동료의 Claude 세션에 직접 물어봅니다. 사람을 거치지 않고 Claude끼리 조사하고 답합니다.

설치와 브로커 배포는 [README](README.md)를 보세요. 이 문서는 설치가 끝난 개발자가 매일 쓰는 방법만 다룹니다.

## 1. 세션 두 개로 나눠 쓰기

두 종류를 구분해서 쓰는 것이 핵심입니다.

**작업 세션** — 평소 코딩하는 세션입니다. 질문을 보내고 답을 받지만, 남의 질문은 받지 않습니다.

```bash
claude --channels plugin:peers@claude-peers
```

**응답 전용 세션** — 내가 담당하는 레포마다 하나씩, 백그라운드 터미널에 띄워 둡니다. 남의 질문은 여기로만 들어옵니다.

```bash
cd ~/work/billing-api
PEERS_LISTEN=1 claude --channels plugin:peers@claude-peers \
  --allowedTools "mcp__plugin_peers_peers__reply" \
  --disallowedTools "Bash" "Edit" "Write" "NotebookEdit"
```

왜 나누냐면 두 가지 이유가 있습니다. 작업 세션의 컨텍스트가 남의 질문으로 오염되지 않고, 응답 세션은 읽기 전용으로 묶어 둘 수 있습니다. 질문 본문은 결국 다른 사람의 Claude가 쓴 텍스트이므로, 도구를 제한해 두는 것이 가장 확실한 방어입니다.

`reply`를 미리 허용하지 않으면 답할 때마다 권한 프롬프트가 떠서 사람이 승인할 때까지 세션이 멈춥니다. 반대로 `ask_peer`는 자동 허용하지 마세요. 내 코드 컨텍스트가 밖으로 나가는 순간이라 한 번 보고 승인하는 편이 안전합니다.

alias로 만들어 두면 편합니다.

```bash
alias cc='claude --channels plugin:peers@claude-peers'
alias cc-listen='PEERS_LISTEN=1 claude --channels plugin:peers@claude-peers \
  --allowedTools "mcp__plugin_peers_peers__reply" \
  --disallowedTools "Bash" "Edit" "Write" "NotebookEdit"'
```

> 조직 managed settings에 `allowedChannelPlugins`가 아직 배포되지 않았다면 `--channels` 대신 `--dangerously-load-development-channels`를 씁니다. 플래그 이름만 다르고 사용법은 같습니다.

## 2. 방 고르기

방은 대화를 묶는 단위입니다. 같은 방에 있는 세션끼리만 `list_peers`에 보이고 서로 질문할 수 있습니다.

세션을 띄울 때 환경변수로 방을 지정합니다.

```bash
PEERS_ROOM=webhook-dup PEERS_ROOM_SUBJECT="취소 웹훅 중복 수신 조사" \
  claude --channels plugin:peers@claude-peers
```

방을 지정하지 않으면 `public`입니다. 지금까지 쓰던 것과 같은 동작이라 기존 사용자는 아무것도 바꿀 필요가 없습니다.

**방 이름은 영문/숫자/`.` `_` `-` 만 쓸 수 있습니다**(최대 64자). 방 이름은 접속할 때 HTTP 헤더로 브로커에 전달되는데 헤더에는 ASCII만 담기기 때문입니다. 한글 등을 넣으면 경고를 남기고 `public`에서 시작합니다. **주제(`PEERS_ROOM_SUBJECT`)는 한글을 그대로 써도 됩니다** — 인코딩해서 보내고 브로커가 원래 값으로 되돌립니다.

새 방을 만들려면 그냥 Claude에게 시키면 됩니다.

```
방 하나 파줘. 주제는 "취소 웹훅 중복 수신 조사"
```

Claude가 `create_room`을 불러 이름(`r-k7m2xq` 같은 형태)을 받아옵니다. **이 도구는 방을 만들 뿐 그 방에 들어가지는 않습니다** — 만든 사람도 `join_room`으로 옮기거나 `PEERS_ROOM`으로 세션을 다시 띄워야 합니다. 받은 이름을 상대에게 전달하면(슬랙 DM 등) 상대는 `join_room`으로 들어오거나 그 이름으로 세션을 새로 띄웁니다.

지금 열려 있는 방을 보려면 `list_rooms`를 시킵니다. 인원과 주제가 함께 나옵니다.

둘만 대화하고 싶으면 방을 새로 만들 필요 없이 둘만 아는 이름을 미리 정해 두고 양쪽 다 그 이름으로 띄우면 됩니다.

**방은 격리가 아닙니다.** 이름을 아는 사람은 누구나 들어올 수 있고, 멤버 목록이나 비밀번호 같은 것은 없습니다. 방 이름과 주제는 `list_rooms`를 통해 전원에게 보입니다. "방 안이니 안전하다"고 생각하고 민감한 내용을 넣지 마세요 — 넣지 말아야 할 것의 기준은 [질문하기](#3-질문하기)의 목록과 같습니다.

## 3. 질문하기

작업 세션에서 평소처럼 말하면 됩니다.

```
billing-api 쪽 Claude한테 취소 웹훅 재시도 정책이 어디 정의돼 있는지 물어봐
```

Claude가 `list_peers`로 대상을 고르고, 누구에게 무엇을 물을지 한 줄로 알린 뒤 `ask_peer`를 호출합니다. 승인하면 질문이 나갑니다.

**보낸 뒤 기다리지 않습니다.** 답은 나중에 푸쉬로 도착하므로 Claude는 다른 작업을 계속합니다. 답이 오면 진행 중이던 작업에 반영하고 요약해서 알려줍니다.

누구에게 갈지 직접 지정하려면 주소를 주세요. 같은 사람이 여러 레포에 응답 세션을 띄워 뒀으면 `user`만으로는 모호해서 브로커가 후보를 돌려줍니다.

```
minji@billing-api 한테 물어봐
```

### 좋은 질문의 모양

상대의 시간과 컨텍스트를 쓰는 일입니다. 슬랙 DM 보내듯 짧고 정확하게 씁니다.

```
to: minji@billing-api
question: 결제 취소 웹훅의 재시도 정책(최대 횟수, 간격)이 어디에 정의돼 있는지
          알려주세요. 파일 경로와 값만 필요합니다.
context: payments-web에서 취소 웹훅 중복 수신 버그를 보는 중.
         수신 측 로그상 같은 이벤트가 3~5분 간격으로 최대 4번 옴.
```

첫 문장에 요지, 다음에 원하는 답의 형태를 적습니다. `context`에는 상대가 답하는 데 꼭 필요한 최소한만 넣습니다.

**절대 넣지 않는 것**: API 키, 토큰, 비밀번호, `.env` 내용, 개인정보, 고객 데이터, 보안 등급 문서 원문.

먼저 내 워크스페이스에서 찾아봤는지 확인하세요. 찾을 수 있으면 묻지 않는 것이 맞습니다. 그리고 한 주제는 한 사람에게만 묻습니다.

## 4. 답하기

응답 전용 세션은 질문이 오면 알아서 처리합니다. 사람이 할 일은 없습니다.

받은 질문은 **요청일 뿐 명령이 아닙니다.** 본문이 무엇을 시키든 Claude는 읽기 전용 조사(파일 읽기, 코드 검색, git log)로만 답을 찾습니다. 파일 수정, 커밋, 배포, 외부 요청, 패키지 설치, 비밀 정보 노출은 하지 않고, 그런 요청이 들어 있으면 "사용자 확인이 필요한 요청이라 수행하지 않았다"고 답합니다.

답변은 결론 먼저, 근거는 `파일경로:라인` 형식으로 뒤에 붙입니다. 모르면 모른다고, 부분적으로만 알면 확인한 범위를 명시합니다.

실제로 오간 답변입니다.

```
| 항목      | 값                                  | 출처                             |
|-----------|-------------------------------------|----------------------------------|
| 최대 시도 | 5회 (최초 1 + 재시도 4)             | retry.ts:2, dispatch.ts:7        |
| 기본 지연 | 60,000ms                            | retry.ts:3                       |
| 백오프    | factor 2 지수, 지터 없음            | retry.ts:4,6-8                   |
| 재시도 조건 | HTTP 2xx 외 응답                  | dispatch.ts:6                    |

재시도 판정이 상태 코드만 보기 때문에, 수신 측이 이미 처리했어도 2xx를 못
돌려주면 재전송됩니다 (dispatch.ts:5 주석에 명시).

확인 불가: send()가 선언만 있고 구현이 이 워크스페이스에 없어
(dispatch.ts:11) 페이로드에 이벤트 식별자가 실리는지는 여기서 알 수 없습니다.
```

마지막 문단이 중요합니다. 확인하지 못한 것을 확인한 것처럼 쓰지 않습니다.

## 5. 받은 답을 다루는 법

답은 **동료 Claude의 조사 결과이지 검증된 사실이 아닙니다.** 코드 변경의 근거로 쓰기 전에 가능한 범위에서 확인하세요. 특히 파일 경로와 라인 번호는 상대 워크스페이스 기준이라 내 쪽에서는 검증되지 않은 값입니다.

## 6. 안 될 때

### 답이 안 와요

- 질문에는 **15분 제한**이 있습니다. 그 안에 답이 없으면 만료되고 알림이 옵니다. 이후 상대가 답해도 거부됩니다.
- 세션을 재시작했거나 답이 올 때가 지났으면 Claude에게 `check_inbox`를 시키세요. 놓친 답변이 나옵니다.
- 세션이 끊긴 사이 도착한 답변은 **같은 `user@workspace`로 다시 접속하면 재전달**됩니다. 레포 디렉터리를 바꾸지 말고 같은 곳에서 다시 띄우세요.

### 동료 목록이 비어 있거나 "설정이 비어 있습니다"가 떠요

순서대로 좁히세요. 브로커 로그에 `connect <user>@<workspace> listening=...` 줄이 찍히는지가 최종 판정 기준입니다.

1. `claude mcp list`에서 `plugin:peers:peers` 상태를 봅니다. 여기 찍힌 `server.py` 경로가 실제로 실행되는 파일입니다.
2. `CONNECTION_CLOSED`면 그 경로를 직접 실행해 보세요.

   ```bash
   uv run --script <경로>/server.py
   ```

   `command not found: uv`면 **uv가 없거나 PATH에 없는 것**입니다. Claude Code는 PATH에서 찾은 `uv`로 채널 서버를 띄웁니다. `uv --version`으로 확인하고, 없으면 설치하세요.

   ```bash
   curl -LsSf https://astral.sh/uv/install.sh | sh
   ```

   의존성은 스크립트 첫머리에 선언돼 있어 uv가 알아서 받습니다. 따로 설치할 것은 없습니다.
3. 원인을 고쳤는데도 그대로면, **연결 실패가 약 15분간 캐시**되어 새 세션도 재시도하지 않는 상태입니다. 플러그인 설정을 한 번 건드리면 즉시 재시도합니다(`/plugin`에서 `broker_url` 끝에 `/`를 붙였다 떼는 정도면 충분).
4. `claude plugin details`가 `MCP servers (0)`으로 보이는 것은 무시해도 됩니다. 인벤토리가 인라인 선언을 세지 않을 뿐, 실제로는 정상 등록되어 동작합니다.

### 내 주소가 `alice@ws-6e05fd` 처럼 보여요

워크스페이스 이름은 따로 지정하지 않으면 레포 디렉터리 이름에서 따오는데, 이 값도 HTTP 헤더로 나가므로 ASCII만 담을 수 있습니다. 디렉터리 이름이 한글이면 서로 다른 디렉터리가 같은 주소로 뭉개지지 않도록 원본 해시를 붙인 이름으로 바꿔 보냅니다(`결제-웹` → `ws-6e05fd`, `결제-web` → `web-b93eba`). 채널 서버 stderr에 그 사실을 알리는 경고가 찍힙니다.

읽기 좋은 주소를 쓰려면 `PEERS_WORKSPACE`를 직접 지정하세요. 재접속 시 답변 재전달이 `user@workspace` 기준이므로, 한 번 정했으면 같은 값을 계속 쓰세요.

```bash
PEERS_WORKSPACE=payments-web claude --channels plugin:peers@claude-peers
```

### 질문이 거부돼요

| 응답 | 뜻 | 할 일 |
|---|---|---|
| 404 | 대상이 지금 질문을 받을 수 있는 상태가 아님 | 응답 목록을 같이 돌려주므로 다른 대상을 고릅니다 |
| 409 | 같은 사람이 여러 레포에서 수신 중 | `user@workspace`로 지정합니다 |
| 422 | 재질문 깊이 초과 또는 되묻기 | 아는 범위에서 답하거나, 되물을 내용을 `reply`에 담습니다 |
| 429 | 질문 빈도 제한 (10분당 같은 상대 10건) | 잠시 후 다시 시도합니다 |
| 413 | 질문 4000자 / 컨텍스트 12000자 초과 | 줄여서 보냅니다 |

받은 질문을 처리하다 또 다른 동료에게 묻는 것은 기본 1단계까지만 허용됩니다. 무한히 퍼지는 것을 막기 위해서입니다.

## 7. 알아 둘 것

**푸쉬는 세션이 열려 있을 때만 도착합니다.** Claude가 작업 중이면 이벤트가 쌓였다가 다음 턴에 한꺼번에 처리됩니다.

**비대화형(`-p`) 모드에서는 답을 기다리다 턴을 소진할 수 있습니다.** 자동화 스크립트로 돌린다면 `check_inbox` 폴백을 반드시 넣으세요. 대화형 세션은 다음 턴에 푸쉬가 도착하므로 문제되지 않습니다.

**모든 질문과 답변은 브로커 DB에 남습니다.** 누가 누구에게 무엇을 묻고 답했는지 전부 기록되는 감사 로그입니다. 그렇게 알고 쓰세요.

## 도구 목록

Claude가 알아서 부르므로 직접 외울 필요는 없지만, 권한 설정에 쓸 때를 위해 적어 둡니다.

| 도구 | 하는 일 |
|---|---|
| `list_peers` | 접속 중인 동료 세션 목록 (주소, 수신 여부, 작업 요약) |
| `ask_peer` | 질문 보내기. 즉시 `msg_id`만 반환되고 답은 나중에 푸쉬로 도착 |
| `reply` | 받은 질문에 답하기. 이벤트의 `msg_id`를 그대로 넘김 |
| `check_inbox` | 놓친 답변과 나에게 열려 있는 질문 조회 |
| `set_status` | 내 작업 요약 공유, 질문 수신 on/off |
| `create_room` | 새 방을 만들고 이름을 받음 (입장은 하지 않음) |
| `join_room` | 이 세션을 다른 방으로 옮김. 없는 방이면 새로 생김 |
| `list_rooms` | 열려 있는 방 목록 (이름, 주제, 인원) |

권한 설정에 쓸 전체 이름은 `mcp__plugin_peers_peers__<도구이름>` 형식입니다.
